import { small } from "@/utils/textSize";


function BurgerLinks() {
  return (
    <p className={`text-blue-600 mb-2 ${small}`}>
      burgerLinks
      <span className="text-gray-400">last</span>
    </p>
  );
}

export default BurgerLinks;
